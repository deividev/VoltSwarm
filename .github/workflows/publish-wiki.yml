name: Notify Wiki of content update

on:
  push:
    branches:
      - codex/demo-map1
      - codex/map-2

permissions:
  contents: read

jobs:
  publish-wiki:
    uses: deividev/VoltSwarm-wiki/.github/workflows/publish.yml@803c881f12a837fda3e9d1b6f25817e255651231
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
